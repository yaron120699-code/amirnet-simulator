diff --git a/css/styles.css b/css/styles.css
index e1ab896..d8a7e8d 100644
--- a/css/styles.css
+++ b/css/styles.css
@@ -1,46 +1,107 @@
 /* ============================================================
-   PrepLab Design System · v0.9.4
+   PrepLab Design Language · v2.0
    "A quiet study room" — paper, ink, one scholarly green.
-   No gradients. No blur. No orbs. Typography is the brand.
+   No gradients. No blur. No orbs. Typography carries the brand.
+
+   This file is the shared design system for every PrepLab module
+   (Amirnet today; Mathematics / Physics / Statistics next). It is
+   organized as:
+
+     1. Tokens        — color, type, space, radius, shadow, motion
+     2. Base           — resets, body, focus
+     3. Layout          — app shell, topbar, footer
+     4. Primitives      — card, button, badge, pill, toggle, input
+     5. Data display    — stat, progress, metric row
+     6. Home            — hero, module grid, continue-learning
+     7. Exam            — question card, option/letter-badge
+     8. Results         — score hero, placement, breakdown, review
+     9. Utilities        — RTL/LTR, motion-safety, responsive
    ============================================================ */
 
+/* ============================================================
+   1. TOKENS
+   ============================================================ */
 :root {
-  /* Surfaces */
+  /* ---- Color: surfaces ---- */
   --paper: #FAF9F6;
   --card: #FFFFFF;
   --card-soft: #F4F2EE;
+  --card-soft-2: #EEEBE4;
   --line: #E8E5DF;
   --line-strong: #D9D5CD;
 
-  /* Ink */
+  /* ---- Color: ink ---- */
   --ink: #1C1B1A;
   --ink-soft: #6B675F;
   --ink-faint: #9A958B;
 
-  /* Accent — deep scholarly green */
+  /* ---- Color: accent (professional, scholarly green) ---- */
   --accent: #14665B;
   --accent-ink: #0E4A42;
   --accent-soft: #E7F0EE;
   --accent-line: #BFD8D3;
 
-  /* Semantic */
+  /* ---- Color: semantic ---- */
   --good: #1D7A4F;
   --good-soft: #E8F3EC;
   --bad: #B3372F;
   --bad-soft: #F9ECEA;
-  --warn: #B26A00;
-
-  /* Shape & depth */
-  --r-lg: 16px;
-  --r-md: 12px;
-  --r-sm: 8px;
-  --shadow: 0 1px 2px rgba(28, 27, 26, 0.05), 0 4px 16px rgba(28, 27, 26, 0.04);
-
-  /* Type */
+  --warn: #9A6300;
+  --warn-soft: #FBF3E5;
+  --warn-line: #EAD9BD;
+
+  /* ---- Shape ---- */
+  --r-xs: 7px;
+  --r-sm: 10px;
+  --r-md: 14px;
+  --r-lg: 20px;
+  --r-full: 999px;
+
+  /* ---- Elevation ---- */
+  --shadow-xs: 0 1px 2px rgba(28, 27, 26, 0.04);
+  --shadow-sm: 0 1px 2px rgba(28, 27, 26, 0.05), 0 4px 16px rgba(28, 27, 26, 0.04);
+  --shadow-md: 0 2px 4px rgba(28, 27, 26, 0.05), 0 12px 32px rgba(28, 27, 26, 0.07);
+  --shadow: var(--shadow-sm); /* back-compat alias */
+
+  /* ---- Space scale (4px base) ---- */
+  --sp-1: 4px;
+  --sp-2: 8px;
+  --sp-3: 12px;
+  --sp-4: 16px;
+  --sp-5: 20px;
+  --sp-6: 24px;
+  --sp-7: 32px;
+  --sp-8: 40px;
+  --sp-9: 56px;
+  --sp-10: 72px;
+
+  /* ---- Type ---- */
   --font-ui: "Inter", "Heebo", system-ui, -apple-system, "Segoe UI", sans-serif;
   --font-display: "Fraunces", "Heebo", Georgia, serif;
+
+  --text-xs: 12px;
+  --text-sm: 13.5px;
+  --text-base: 15.5px;
+  --text-md: 17px;
+  --text-lg: 20px;
+  --text-xl: clamp(24px, 3vw, 30px);
+  --text-2xl: clamp(28px, 4vw, 40px);
+  --text-3xl: clamp(40px, 6vw, 60px);
+  --text-4xl: clamp(56px, 9vw, 84px);
+
+  /* ---- Motion (150–200ms, per brief) ---- */
+  --ease: cubic-bezier(0.22, 1, 0.36, 1);
+  --motion-fast: 150ms;
+  --motion-base: 200ms;
+
+  /* ---- Layout ---- */
+  --content-width: clamp(1100px, 92vw, 1280px);
+  --content-width-reading: 860px; /* exam / results: narrower for focus */
 }
 
+/* ============================================================
+   2. BASE
+   ============================================================ */
 * { box-sizing: border-box; }
 html { scroll-behavior: smooth; }
 
@@ -58,32 +119,76 @@ body {
 
 body.rtl { font-family: "Heebo", var(--font-ui); }
 
-/* ---------- Shell ---------- */
+:focus-visible {
+  outline: 2px solid var(--accent);
+  outline-offset: 2px;
+  border-radius: var(--r-sm);
+}
+
+h1, h2 {
+  margin: var(--sp-2) 0 var(--sp-3);
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: var(--text-2xl);
+  letter-spacing: -0.015em;
+  line-height: 1.15;
+}
+
+h3 {
+  margin: var(--sp-7) 0 var(--sp-3);
+  font-size: var(--text-base);
+  font-weight: 650;
+  letter-spacing: -0.005em;
+  color: var(--ink);
+}
 
+p { margin: 0 0 var(--sp-1); }
+
+.muted-note {
+  color: var(--ink-soft);
+  line-height: 1.7;
+  font-size: var(--text-base);
+  margin: 0 0 4px;
+}
+
+.eyebrow {
+  display: block;
+  color: var(--accent);
+  text-transform: uppercase;
+  letter-spacing: 0.12em;
+  font-size: var(--text-xs);
+  font-weight: 700;
+  margin-bottom: var(--sp-1);
+}
+
+body.rtl .eyebrow { text-transform: none; letter-spacing: 0.04em; font-size: 12px; }
+
+/* ============================================================
+   3. LAYOUT — app shell / topbar / footer
+   ============================================================ */
 .app-shell {
-  max-width: 1040px;
+  max-width: var(--content-width);
   margin: 0 auto;
   min-height: 100vh;
-  padding: 20px 24px;
+  padding: var(--sp-5) var(--sp-6);
   display: flex;
   flex-direction: column;
-  gap: 28px;
+  gap: var(--sp-7);
 }
 
 .topbar {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  gap: 16px;
-  padding: 6px 0;
+  gap: var(--sp-4);
+  padding: 6px 0 var(--sp-4);
   border-bottom: 1px solid var(--line);
-  padding-bottom: 16px;
 }
 
 .brand {
   display: flex;
   align-items: center;
-  gap: 12px;
+  gap: var(--sp-3);
   color: var(--ink);
   text-decoration: none;
 }
@@ -93,12 +198,13 @@ body.rtl { font-family: "Heebo", var(--font-ui); }
   place-items: center;
   width: 38px;
   height: 38px;
-  border-radius: 10px;
+  border-radius: var(--r-sm);
   background: var(--ink);
   color: var(--paper);
   font-weight: 700;
   font-size: 18px;
   font-family: var(--font-display);
+  flex: 0 0 auto;
 }
 
 .brand strong {
@@ -109,21 +215,9 @@ body.rtl { font-family: "Heebo", var(--font-ui); }
   line-height: 1.2;
 }
 
-.eyebrow {
-  display: block;
-  color: var(--accent);
-  text-transform: uppercase;
-  letter-spacing: 0.12em;
-  font-size: 11px;
-  font-weight: 700;
-  margin-bottom: 4px;
-}
-
-body.rtl .eyebrow { text-transform: none; letter-spacing: 0.04em; font-size: 12px; }
-
 .top-actions {
   display: flex;
-  gap: 10px;
+  gap: var(--sp-3);
   align-items: center;
   flex-wrap: wrap;
   justify-content: flex-end;
@@ -133,7 +227,7 @@ body.rtl .eyebrow { text-transform: none; letter-spacing: 0.04em; font-size: 12p
   border: 1px solid var(--line);
   background: var(--card);
   padding: 7px 12px;
-  border-radius: 999px;
+  border-radius: var(--r-full);
   color: var(--ink-soft);
   font-size: 12.5px;
   font-weight: 500;
@@ -148,12 +242,12 @@ body.rtl .eyebrow { text-transform: none; letter-spacing: 0.04em; font-size: 12p
   padding: 3px;
   border: 1px solid var(--line);
   background: var(--card);
-  border-radius: 999px;
+  border-radius: var(--r-full);
 }
 
 .lang-btn {
   padding: 6px 12px;
-  border-radius: 999px;
+  border-radius: var(--r-full);
   font-size: 12.5px;
   font-weight: 550;
   color: var(--ink-soft);
@@ -161,16 +255,26 @@ body.rtl .eyebrow { text-transform: none; letter-spacing: 0.04em; font-size: 12p
   border: 0;
   cursor: pointer;
   font-family: inherit;
-  transition: background 0.15s ease, color 0.15s ease;
+  transition: background var(--motion-fast) ease, color var(--motion-fast) ease;
 }
 
 .lang-btn:hover { color: var(--ink); }
 .lang-btn.active { background: var(--ink); color: var(--paper); }
 
-/* ---------- Screen transitions ---------- */
+footer {
+  margin-top: auto;
+  padding: var(--sp-5) 0 8px;
+  display: flex;
+  justify-content: space-between;
+  gap: var(--sp-3);
+  color: var(--ink-faint);
+  font-size: 12.5px;
+  border-top: 1px solid var(--line);
+}
 
+/* ---------- Screen transitions ---------- */
 main > section {
-  animation: rise 0.35s cubic-bezier(0.22, 1, 0.36, 1);
+  animation: rise 0.32s var(--ease);
 }
 
 @keyframes rise {
@@ -178,54 +282,23 @@ main > section {
   to   { opacity: 1; transform: translateY(0); }
 }
 
-/* ---------- Cards ---------- */
-
+/* ============================================================
+   4. PRIMITIVES — card, button, badge, pill, toggle, input
+   ============================================================ */
 .card {
   background: var(--card);
   border: 1px solid var(--line);
   border-radius: var(--r-lg);
-  padding: 28px;
-  box-shadow: var(--shadow);
-}
-
-.hero {
-  display: grid;
-  grid-template-columns: 1.2fr 0.8fr;
-  gap: 20px;
-  align-items: stretch;
-}
-
-h1, h2 {
-  margin: 8px 0 12px;
-  font-family: var(--font-display);
-  font-weight: 600;
-  font-size: clamp(28px, 4vw, 40px);
-  letter-spacing: -0.015em;
-  line-height: 1.15;
-}
-
-h3 {
-  margin: 32px 0 12px;
-  font-size: 15px;
-  font-weight: 650;
-  letter-spacing: -0.005em;
-  color: var(--ink);
-}
-
-.hero p, .muted-note {
-  color: var(--ink-soft);
-  line-height: 1.7;
-  font-size: 15.5px;
-  margin: 0 0 4px;
+  padding: var(--sp-7);
+  box-shadow: var(--shadow-sm);
 }
 
 /* ---------- Buttons ---------- */
-
 .actions {
   display: flex;
   flex-wrap: wrap;
-  gap: 10px;
-  margin-top: 22px;
+  gap: var(--sp-3);
+  margin-top: var(--sp-6);
   align-items: center;
 }
 
@@ -239,11 +312,12 @@ button, .button-link {
   cursor: pointer;
   color: var(--paper);
   background: var(--ink);
-  transition: background 0.15s ease, transform 0.15s ease, box-shadow 0.15s ease;
+  transition: background var(--motion-fast) ease, transform var(--motion-fast) ease, box-shadow var(--motion-fast) ease, border-color var(--motion-fast) ease;
   text-decoration: none;
   display: inline-flex;
   align-items: center;
   justify-content: center;
+  gap: 8px;
   min-height: 46px;
 }
 
@@ -266,6 +340,15 @@ button.secondary:hover, .button-link:hover {
   box-shadow: none;
 }
 
+button.ghost {
+  background: transparent;
+  color: var(--ink-soft);
+  border-color: transparent;
+  padding: 8px 10px;
+  min-height: 0;
+}
+button.ghost:hover { color: var(--ink); background: var(--card-soft); transform: none; box-shadow: none; }
+
 button:disabled {
   opacity: 0.4;
   cursor: not-allowed;
@@ -273,21 +356,46 @@ button:disabled {
   box-shadow: none;
 }
 
-:focus-visible {
-  outline: 2px solid var(--accent);
-  outline-offset: 2px;
-  border-radius: var(--r-sm);
+/* ---------- Badges & pills ---------- */
+.pill {
+  border: 1px solid var(--line);
+  color: var(--ink-soft);
+  background: var(--card-soft);
+  border-radius: var(--r-full);
+  padding: 4px 11px;
+  font-size: 12px;
+  font-weight: 550;
+  display: inline-flex;
+  align-items: center;
+  gap: 5px;
 }
 
-/* ---------- Toggle ---------- */
+.badge {
+  display: inline-flex;
+  align-items: center;
+  gap: 5px;
+  border-radius: var(--r-full);
+  padding: 3px 10px;
+  font-size: 11.5px;
+  font-weight: 700;
+  letter-spacing: 0.02em;
+  text-transform: uppercase;
+}
+body.rtl .badge { text-transform: none; letter-spacing: 0.01em; }
+
+.badge-soon { background: var(--card-soft-2); color: var(--ink-faint); border: 1px solid var(--line); }
+.badge-new  { background: var(--accent-soft); color: var(--accent-ink); border: 1px solid var(--accent-line); }
+.badge-good { background: var(--good-soft); color: var(--good); }
+.badge-bad  { background: var(--bad-soft); color: var(--bad); }
 
+/* ---------- Toggle ---------- */
 .toggle {
   display: flex;
-  gap: 10px;
+  gap: var(--sp-3);
   align-items: center;
   color: var(--ink-soft);
   font-size: 14px;
-  margin-top: 18px;
+  margin-top: var(--sp-5);
   padding: 11px 14px;
   background: var(--card-soft);
   border: 1px solid var(--line);
@@ -299,13 +407,39 @@ button:disabled {
 
 .toggle input { width: 17px; height: 17px; accent-color: var(--accent); cursor: pointer; }
 
-/* ---------- Stats ---------- */
+/* ---------- Inputs ---------- */
+.field-label {
+  display: grid;
+  gap: 7px;
+  color: var(--ink-soft);
+  font-size: 13px;
+  font-weight: 600;
+  margin-top: var(--sp-4);
+  max-width: 320px;
+}
+
+.institution-select {
+  width: 100%;
+  border: 1px solid var(--line-strong);
+  border-radius: var(--r-md);
+  background: var(--card);
+  color: var(--ink);
+  min-height: 44px;
+  padding: 9px 12px;
+  font: inherit;
+  font-size: 14px;
+  transition: border-color var(--motion-fast) ease;
+}
+.institution-select:hover { border-color: var(--ink-faint); }
 
+/* ============================================================
+   5. DATA DISPLAY — stat, progress, metric row
+   ============================================================ */
 .stats-grid {
   display: grid;
   grid-template-columns: repeat(4, 1fr);
-  gap: 10px;
-  margin: 16px 0;
+  gap: var(--sp-3);
+  margin: var(--sp-4) 0;
 }
 
 .mini-grid { grid-template-columns: 1fr 1fr; }
@@ -331,13 +465,11 @@ button:disabled {
 
 .stat span { font-size: 12px; color: var(--ink-faint); }
 
-/* ---------- Progress ---------- */
-
 .progress {
   width: 100%;
   height: 6px;
   background: var(--line);
-  border-radius: 99px;
+  border-radius: var(--r-full);
   overflow: hidden;
   margin: 14px 0 0;
 }
@@ -347,18 +479,184 @@ button:disabled {
   height: 100%;
   background: var(--accent);
   width: 0%;
-  border-radius: 99px;
-  transition: width 0.4s cubic-bezier(0.22, 1, 0.36, 1);
+  border-radius: var(--r-full);
+  transition: width var(--motion-base) var(--ease);
+}
+
+.progress.slim { height: 5px; margin: 0; }
+
+.review, .metric-list { display: grid; gap: var(--sp-3); }
+
+.review-item, .metric-row {
+  border: 1px solid var(--line);
+  border-radius: var(--r-md);
+  padding: 16px 18px;
+  background: var(--card);
+}
+
+.review-item { font-size: 14.5px; }
+
+.good { color: var(--good); font-weight: 650; }
+.bad { color: var(--bad); font-weight: 650; }
+
+.review-item .good, .review-item .bad {
+  padding: 2px 10px;
+  border-radius: var(--r-full);
+  font-size: 12.5px;
+}
+.review-item .good { background: var(--good-soft); }
+.review-item .bad { background: var(--bad-soft); }
+
+.metric-row > div:first-child {
+  display: flex;
+  justify-content: space-between;
+  gap: var(--sp-3);
+  align-items: center;
+  margin-bottom: 8px;
+  color: var(--ink-soft);
+  font-size: 13.5px;
+}
+
+.metric-row strong { color: var(--ink); font-weight: 600; }
+
+/* ============================================================
+   6. HOME — hero, module grid, continue-learning
+   ============================================================ */
+.hero {
+  display: grid;
+  grid-template-columns: 1.35fr 0.65fr;
+  gap: var(--sp-5);
+  align-items: stretch;
+}
+
+.hero p { color: var(--ink-soft); line-height: 1.7; font-size: var(--text-base); margin: 0 0 4px; }
+body.rtl .hero p { font-size: 16px; line-height: 1.85; }
+
+/* Continue-learning strip: shown above the hero when a prior
+   simulation exists in local telemetry. */
+.continue-card {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--sp-5);
+  flex-wrap: wrap;
+  border: 1px solid var(--accent-line);
+  background: var(--accent-soft);
+  border-radius: var(--r-lg);
+  padding: var(--sp-5) var(--sp-6);
+}
+
+.continue-info { display: flex; align-items: center; gap: var(--sp-5); flex-wrap: wrap; }
+
+.continue-score {
+  font-family: var(--font-display);
+  font-size: 32px;
+  font-weight: 600;
+  color: var(--accent-ink);
+  letter-spacing: -0.02em;
+  line-height: 1;
+}
+
+.continue-meta { display: grid; gap: 2px; }
+.continue-meta strong { font-size: 14px; color: var(--ink); font-weight: 650; }
+.continue-meta span { font-size: 12.5px; color: var(--ink-soft); }
+
+/* Section heading row (label + optional trailing note) used above
+   the module grid and other home sections. */
+.section-head {
+  display: flex;
+  align-items: baseline;
+  justify-content: space-between;
+  gap: var(--sp-4);
+  margin: var(--sp-2) 0 var(--sp-1);
 }
 
-/* ---------- Exam ---------- */
+.section-head h3 { margin: 0; }
+.section-head .muted-note { font-size: 13px; margin: 0; }
+
+/* Module grid: current + future modules (Mathematics, Physics, Statistics) */
+.module-grid {
+  display: grid;
+  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
+  gap: var(--sp-4);
+}
+
+.module-card {
+  position: relative;
+  border: 1px solid var(--line);
+  background: var(--card);
+  border-radius: var(--r-lg);
+  padding: var(--sp-5);
+  display: grid;
+  gap: 10px;
+  align-content: start;
+  transition: border-color var(--motion-base) ease, transform var(--motion-base) ease, box-shadow var(--motion-base) ease;
+}
 
-.exam-card { max-width: 860px; margin: 0 auto; width: 100%; }
+.module-card.is-active {
+  border-color: var(--accent-line);
+  cursor: pointer;
+}
+.module-card.is-active:hover {
+  border-color: var(--accent);
+  transform: translateY(-2px);
+  box-shadow: var(--shadow-md);
+}
+
+.module-card.is-soon { color: var(--ink-faint); }
+.module-card.is-soon .module-icon { background: var(--card-soft-2); color: var(--ink-faint); }
+
+.module-icon {
+  width: 40px;
+  height: 40px;
+  border-radius: var(--r-sm);
+  display: grid;
+  place-items: center;
+  background: var(--accent-soft);
+  color: var(--accent-ink);
+  font-family: var(--font-display);
+  font-weight: 600;
+  font-size: 17px;
+}
+
+.module-card h4 {
+  margin: 0;
+  font-size: 15px;
+  font-weight: 650;
+  color: var(--ink);
+  letter-spacing: -0.005em;
+}
+.module-card.is-soon h4 { color: var(--ink-soft); }
+
+.module-card p { margin: 0; font-size: 13px; color: var(--ink-soft); line-height: 1.55; }
+
+/* Recent activity list on the home page */
+.activity-list { display: grid; gap: 8px; }
+
+.activity-row {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--sp-4);
+  border: 1px solid var(--line);
+  background: var(--card-soft);
+  border-radius: var(--r-md);
+  padding: 12px 16px;
+  font-size: 13.5px;
+}
+
+.activity-row strong { color: var(--ink); font-weight: 650; }
+.activity-row span { color: var(--ink-faint); font-size: 12px; }
+
+/* ============================================================
+   7. EXAM — question card, option / letter-badge
+   ============================================================ */
+.exam-card { max-width: var(--content-width-reading); margin: 0 auto; width: 100%; }
 
 .exam-head {
   display: flex;
   justify-content: space-between;
-  gap: 16px;
+  gap: var(--sp-4);
   align-items: center;
   margin-bottom: 4px;
   color: var(--ink-soft);
@@ -373,33 +671,24 @@ button:disabled {
   background: var(--card-soft);
   border: 1px solid var(--line);
   padding: 5px 12px;
-  border-radius: 999px;
+  border-radius: var(--r-full);
   font-size: 13.5px;
+  transition: color var(--motion-base) ease, background var(--motion-base) ease, border-color var(--motion-base) ease;
 }
 
-.exam-timer.time-low { color: var(--warn); border-color: #EAD9BD; background: #FBF3E5; }
+.exam-timer.time-low { color: var(--warn); border-color: var(--warn-line); background: var(--warn-soft); }
 
 .question-meta {
   display: flex;
   flex-wrap: wrap;
   gap: 6px;
-  margin: 20px 0 4px;
-}
-
-.pill {
-  border: 1px solid var(--line);
-  color: var(--ink-soft);
-  background: var(--card-soft);
-  border-radius: 999px;
-  padding: 4px 11px;
-  font-size: 12px;
-  font-weight: 550;
+  margin: var(--sp-5) 0 4px;
 }
 
 .question-text {
-  font-size: 20px;
+  font-size: var(--text-lg);
   line-height: 1.6;
-  margin: 18px 0 22px;
+  margin: var(--sp-4) 0 var(--sp-5);
   font-weight: 500;
   letter-spacing: -0.005em;
 }
@@ -414,13 +703,12 @@ button:disabled {
   font-size: 15.5px;
   max-height: 320px;
   overflow: auto;
-  margin: 18px 0 4px;
+  margin: var(--sp-4) 0 4px;
 }
 
 .passage strong { font-family: var(--font-display); font-size: 17px; font-weight: 600; }
 
-/* ---------- Options ---------- */
-
+/* ---------- Options: circular letter badges, not plain numbers ---------- */
 .options { display: grid; gap: 9px; }
 
 .option {
@@ -436,33 +724,37 @@ button:disabled {
   border-radius: var(--r-md);
   display: flex;
   align-items: flex-start;
-  gap: 12px;
+  gap: 14px;
   justify-content: flex-start;
-  transition: border-color 0.15s ease, background 0.15s ease;
+  transition: border-color var(--motion-fast) ease, background var(--motion-fast) ease, transform var(--motion-fast) ease, box-shadow var(--motion-fast) ease;
   min-height: 0;
 }
 
 .option:hover {
   background: var(--card-soft);
   border-color: var(--ink-faint);
-  transform: none;
-  box-shadow: none;
+  transform: translateY(-1px);
+  box-shadow: var(--shadow-xs);
 }
+.option:hover .option-letter { border-color: var(--ink-faint); }
+
+.option:active { transform: translateY(0); }
 
-.option-num {
+.option-letter {
   flex: 0 0 auto;
   display: grid;
   place-items: center;
-  width: 24px;
-  height: 24px;
+  width: 28px;
+  height: 28px;
   margin-top: 1px;
-  border-radius: 7px;
-  border: 1px solid var(--line-strong);
+  border-radius: var(--r-full);
+  border: 1.5px solid var(--line-strong);
   background: var(--card-soft);
   color: var(--ink-soft);
-  font-size: 12.5px;
-  font-weight: 650;
-  font-variant-numeric: tabular-nums;
+  font-size: 13px;
+  font-weight: 700;
+  font-family: var(--font-ui);
+  transition: background var(--motion-fast) ease, border-color var(--motion-fast) ease, color var(--motion-fast) ease, transform var(--motion-fast) ease;
 }
 
 .option.selected {
@@ -471,20 +763,20 @@ button:disabled {
   box-shadow: inset 0 0 0 1px var(--accent);
 }
 
-.option.selected .option-num {
+.option.selected .option-letter {
   background: var(--accent);
   border-color: var(--accent);
   color: #fff;
+  transform: scale(1.08);
 }
 
 /* ---------- Exam nav ---------- */
-
 .nav {
   display: flex;
   justify-content: space-between;
-  gap: 12px;
-  margin-top: 26px;
-  padding-top: 20px;
+  gap: var(--sp-3);
+  margin-top: var(--sp-6);
+  padding-top: var(--sp-5);
   border-top: 1px solid var(--line);
   align-items: center;
 }
@@ -495,13 +787,14 @@ button:disabled {
   font-weight: 450;
 }
 
-/* ---------- Results ---------- */
-
-.results-card { max-width: 860px; margin: 0 auto; width: 100%; }
+/* ============================================================
+   8. RESULTS — score hero, placement, breakdown, review
+   ============================================================ */
+.results-card { max-width: var(--content-width-reading); margin: 0 auto; width: 100%; }
 
 .score-number {
   font-family: var(--font-display);
-  font-size: clamp(56px, 9vw, 84px);
+  font-size: var(--text-4xl);
   font-weight: 600;
   margin: 4px 0 8px;
   letter-spacing: -0.03em;
@@ -509,47 +802,6 @@ button:disabled {
   color: var(--accent-ink);
 }
 
-.review, .metric-list { display: grid; gap: 10px; }
-
-.review-item, .metric-row {
-  border: 1px solid var(--line);
-  border-radius: var(--r-md);
-  padding: 16px 18px;
-  background: var(--card);
-}
-
-.review-item { font-size: 14.5px; }
-
-.good { color: var(--good); font-weight: 650; }
-.bad { color: var(--bad); font-weight: 650; }
-
-.review-item .good {
-  background: var(--good-soft);
-  padding: 2px 10px;
-  border-radius: 999px;
-  font-size: 12.5px;
-}
-
-.review-item .bad {
-  background: var(--bad-soft);
-  padding: 2px 10px;
-  border-radius: 999px;
-  font-size: 12.5px;
-}
-
-.metric-row > div:first-child {
-  display: flex;
-  justify-content: space-between;
-  gap: 12px;
-  align-items: center;
-  margin-bottom: 8px;
-  color: var(--ink-soft);
-  font-size: 13.5px;
-}
-
-.metric-row strong { color: var(--ink); font-weight: 600; }
-.progress.slim { height: 5px; margin: 0; }
-
 .ability-path {
   display: flex;
   flex-wrap: wrap;
@@ -577,82 +829,49 @@ button:disabled {
 
 .review-question { margin-top: 10px; color: var(--ink); font-weight: 550; }
 
-/* ---------- Direction handling ---------- */
-
-.english-content { direction: ltr; text-align: left; }
-body.rtl .topbar, body.rtl .brand { direction: ltr; }
-body.rtl .hero, body.rtl .card, body.rtl footer { direction: rtl; }
-body.rtl .option { text-align: left; }
-body.rtl .exam-head, body.rtl .metric-row > div:first-child { direction: rtl; }
-body.rtl .version { direction: ltr; }
-body.rtl .hero p { font-size: 16px; line-height: 1.85; }
-
-/* ---------- Footer ---------- */
-
-footer {
-  margin-top: auto;
-  padding: 20px 0 8px;
+/* Recommendations block */
+.recommend-list { display: grid; gap: 8px; }
+.recommend-row {
   display: flex;
-  justify-content: space-between;
+  align-items: flex-start;
   gap: 12px;
-  color: var(--ink-faint);
-  font-size: 12.5px;
-  border-top: 1px solid var(--line);
-}
-
-/* ---------- Motion preferences ---------- */
-
-@media (prefers-reduced-motion: reduce) {
-  *, *::before, *::after {
-    animation-duration: 0.01ms !important;
-    transition-duration: 0.01ms !important;
-  }
-}
-
-/* ---------- Mobile ---------- */
-
-@media (max-width: 760px) {
-  .app-shell { padding: 14px 16px; gap: 20px; }
-  .hero { grid-template-columns: 1fr; }
-  .stats-grid { grid-template-columns: repeat(2, 1fr); }
-  .card { padding: 20px; }
-  .topbar { flex-direction: column; align-items: flex-start; }
-  .top-actions { justify-content: flex-start; }
-  .question-text { font-size: 18px; }
-  .metric-row > div:first-child { flex-direction: column; align-items: flex-start; }
-  .nav { flex-direction: column-reverse; }
-  .nav .actions { width: 100%; margin: 0; }
-  button, .button-link { width: 100%; }
-  .toggle { width: 100%; }
-  footer { flex-direction: column; gap: 4px; }
+  border: 1px solid var(--line);
+  border-radius: var(--r-md);
+  background: var(--card);
+  padding: 14px 16px;
+  font-size: 14px;
+  color: var(--ink);
 }
-
-/* ---------- Institution / placement ---------- */
-.field-label {
+.recommend-icon {
+  flex: 0 0 auto;
+  width: 26px;
+  height: 26px;
+  border-radius: var(--r-full);
   display: grid;
-  gap: 7px;
-  color: var(--ink-soft);
+  place-items: center;
+  background: var(--accent-soft);
+  color: var(--accent-ink);
   font-size: 13px;
-  font-weight: 600;
-  margin-top: 14px;
-  max-width: 320px;
+  font-weight: 700;
 }
 
-.institution-select {
-  width: 100%;
-  border: 1px solid var(--line-strong);
-  border-radius: var(--r-md);
-  background: var(--card);
-  color: var(--ink);
-  min-height: 44px;
-  padding: 9px 12px;
-  font: inherit;
-  font-size: 14px;
+/* Next-step CTA row */
+.next-step {
+  display: flex;
+  flex-wrap: wrap;
+  gap: var(--sp-3);
+  align-items: center;
+  justify-content: space-between;
+  margin-top: var(--sp-6);
+  padding-top: var(--sp-5);
+  border-top: 1px solid var(--line);
 }
+.next-step .muted-note { margin: 0; max-width: 420px; }
 
+/* Placement / institution */
 .placement-card {
-  margin: 18px 0;
-  padding: 18px;
+  margin: var(--sp-5) 0;
+  padding: var(--sp-5);
   border: 1px solid var(--accent-line);
   background: var(--accent-soft);
   border-radius: var(--r-lg);
@@ -661,14 +880,14 @@ footer {
 .placement-main {
   display: flex;
   justify-content: space-between;
-  gap: 16px;
+  gap: var(--sp-4);
   align-items: flex-start;
 }
 
 .placement-title {
   margin: 4px 0 6px;
   font-family: var(--font-display);
-  font-size: clamp(24px, 4vw, 34px);
+  font-size: var(--text-xl);
   color: var(--accent-ink);
 }
 
@@ -699,9 +918,9 @@ footer {
   direction: ltr;
   display: grid;
   grid-template-columns: auto 1fr auto;
-  gap: 10px;
+  gap: var(--sp-3);
   align-items: center;
-  margin: 16px 0 10px;
+  margin: var(--sp-4) 0 10px;
 }
 
 .scale-end {
@@ -715,7 +934,7 @@ footer {
   position: relative;
   height: 7px;
   background: var(--line-strong);
-  border-radius: 99px;
+  border-radius: var(--r-full);
 }
 
 .scale-dot {
@@ -723,7 +942,7 @@ footer {
   top: 50%;
   width: 17px;
   height: 17px;
-  border-radius: 99px;
+  border-radius: var(--r-full);
   background: var(--accent);
   border: 3px solid var(--card);
   box-shadow: 0 0 0 1px var(--accent);
@@ -739,20 +958,62 @@ footer {
   opacity: 0.55;
 }
 
-.small-note {
-  font-size: 12.5px;
-  margin-top: 6px;
+.small-note { font-size: 12.5px; margin-top: 6px; }
+
+/* ============================================================
+   9. UTILITIES — RTL/LTR, motion-safety, responsive
+   ============================================================ */
+.english-content { direction: ltr; text-align: left; }
+body.rtl .topbar, body.rtl .brand { direction: ltr; }
+body.rtl .hero, body.rtl .card, body.rtl footer { direction: rtl; }
+body.rtl .option { text-align: left; }
+body.rtl .exam-head, body.rtl .metric-row > div:first-child { direction: rtl; }
+body.rtl .version { direction: ltr; }
+body.rtl .continue-card, body.rtl .continue-info { direction: rtl; }
+
+@media (prefers-reduced-motion: reduce) {
+  *, *::before, *::after {
+    animation-duration: 0.01ms !important;
+    transition-duration: 0.01ms !important;
+  }
+}
+
+/* ---------- Mobile: designed intentionally, not just shrunk ---------- */
+@media (max-width: 980px) {
+  .app-shell { padding: var(--sp-4) var(--sp-5); }
+  .module-grid { grid-template-columns: repeat(2, 1fr); }
 }
 
 @media (max-width: 760px) {
+  .app-shell { padding: 14px 16px; gap: var(--sp-5); }
+  .hero { grid-template-columns: 1fr; }
+  .stats-grid { grid-template-columns: repeat(2, 1fr); }
+  .module-grid { grid-template-columns: 1fr 1fr; }
+  .card { padding: var(--sp-5); }
+  .topbar { flex-direction: column; align-items: flex-start; gap: 10px; }
+  .top-actions { justify-content: flex-start; }
+  .question-text { font-size: 18px; }
+  .metric-row > div:first-child { flex-direction: column; align-items: flex-start; }
+  .nav { flex-direction: column-reverse; }
+  .nav .actions { width: 100%; margin: 0; }
+  button, .button-link { width: 100%; }
+  .toggle { width: 100%; }
+  footer { flex-direction: column; gap: 4px; }
+  .continue-card { flex-direction: column; align-items: stretch; }
+  .continue-info { justify-content: space-between; }
+  .next-step { flex-direction: column; align-items: stretch; }
   .field-label { max-width: 100%; }
   .placement-main { flex-direction: column; }
   .placement-target { width: 100%; }
 }
 
+@media (max-width: 480px) {
+  .module-grid { grid-template-columns: 1fr; }
+}
+
 /* ---------- Developer Debug Mode ---------- */
 .debug-panel {
-  margin-top: 16px;
+  margin-top: var(--sp-4);
   border: 1px dashed var(--line-strong);
   background: #fffdf8;
   border-radius: var(--r-md);
diff --git a/js/app.js b/js/app.js
index 1f4ace5..a3bc676 100644
--- a/js/app.js
+++ b/js/app.js
@@ -68,7 +68,29 @@ const I18N = {
     avgRestatementTime: "Restatement",
     fastestQuestion: "Fastest Question",
     slowestQuestion: "Slowest Question",
-    untimedTotal: "Untimed"
+    untimedTotal: "Untimed",
+    continueLearning: "Continue learning",
+    lastSimulation: "Your last simulation",
+    resume: "Practice again",
+    modules: "Modules",
+    modulesNote: "One design system, every subject.",
+    modAmirnetDesc: "Adaptive sentence completion, restatement, and reading comprehension.",
+    modMathDesc: "Adaptive algebra, geometry, and word-problem practice.",
+    modPhysicsDesc: "Mechanics, energy, and waves with worked solutions.",
+    modStatsDesc: "Probability, distributions, and data interpretation.",
+    comingSoon: "Coming soon",
+    recentActivity: "Recent activity",
+    recommendations: "Recommendations",
+    nextStep: "Next step",
+    nextStepNote: "Keep your ability estimate fresh — most students improve fastest with short, frequent sessions.",
+    practiceAgain: "Practice again",
+    recWeak: "Your accuracy was lowest in",
+    recWeakTail: "— a short focused session there should move the needle fastest.",
+    recStrong: "Strongest area:",
+    recStrongTail: "keep it sharp with occasional review.",
+    recPace: "You averaged",
+    recPaceTail: "per question — pacing looks steady for the full exam.",
+    ago: "ago"
   },
   he: {
     dir: "rtl",
@@ -133,7 +155,29 @@ const I18N = {
     avgRestatementTime: "משפטים נרדפים",
     fastestQuestion: "השאלה המהירה ביותר",
     slowestQuestion: "השאלה האיטית ביותר",
-    untimedTotal: "ללא הגבלת זמן"
+    untimedTotal: "ללא הגבלת זמן",
+    continueLearning: "המשך למידה",
+    lastSimulation: "הסימולציה האחרונה שלך",
+    resume: "תרגול נוסף",
+    modules: "מודולים",
+    modulesNote: "שפת עיצוב אחת, לכל מקצוע.",
+    modAmirnetDesc: "השלמת משפטים, ניסוח מחדש והבנת הנקרא — אדפטיבי.",
+    modMathDesc: "אלגברה, גאומטריה ותרגול בעיות מילוליות אדפטיבי.",
+    modPhysicsDesc: "מכניקה, אנרגיה וגלים עם פתרונות מלאים.",
+    modStatsDesc: "הסתברות, התפלגויות וניתוח נתונים.",
+    comingSoon: "בקרוב",
+    recentActivity: "פעילות אחרונה",
+    recommendations: "המלצות",
+    nextStep: "הצעד הבא",
+    nextStepNote: "שמור על הערכת היכולת שלך רעננה — רוב הסטודנטים משתפרים הכי מהר עם תרגול קצר ותכוף.",
+    practiceAgain: "תרגול נוסף",
+    recWeak: "הדיוק שלך היה הנמוך ביותר ב",
+    recWeakTail: "— תרגול ממוקד וקצר שם צפוי לשפר הכי מהר.",
+    recStrong: "התחום החזק ביותר:",
+    recStrongTail: "שמור על החדות עם חזרה מדי פעם.",
+    recPace: "משך הזמן הממוצע שלך היה",
+    recPaceTail: "לשאלה — הקצב נראה יציב לקראת המבחן המלא.",
+    ago: "לפני"
   }
 };
 
@@ -222,29 +266,100 @@ function applyLanguageChrome() {
   }
 }
 
+// Relative "X ago" string for the continue-learning card. UI-only —
+// reads timestamps already recorded by the telemetry read path.
+function timeAgo(iso) {
+  const diffMs = Date.now() - new Date(iso).getTime();
+  const mins = Math.round(diffMs / 60000);
+  if (mins < 1) return state.lang === "he" ? "כרגע" : "just now";
+  if (mins < 60) return `${mins}${state.lang === "he" ? " דק׳ " : "m "}${t("ago")}`;
+  const hours = Math.round(mins / 60);
+  if (hours < 24) return `${hours}${state.lang === "he" ? " שע׳ " : "h "}${t("ago")}`;
+  const days = Math.round(hours / 24);
+  return `${days}${state.lang === "he" ? " ימים " : "d "}${t("ago")}`;
+}
+
+// Reads the most recent completed simulation from the existing
+// telemetry log (js/telemetry.js) — no new scoring/adaptive logic,
+// purely surfacing data that is already recorded on finishExam().
+function continueLearningCard() {
+  if (!window.PrepLabTelemetry) return "";
+  let last;
+  try { last = PrepLabTelemetry.lastSimulations(1)[0]; } catch (e) { last = null; }
+  if (!last) return "";
+  const placement = window.PrepLabPlacement ? PrepLabPlacement.classify(last.score, state.lang, last.confidence) : null;
+  return `
+    <div class="continue-card">
+      <div class="continue-info">
+        <div class="continue-score">${last.score}</div>
+        <div class="continue-meta">
+          <strong>${placement ? placement.levelLabel : t("lastSimulation")}</strong>
+          <span>${t("lastSimulation")} · ${timeAgo(last.at)}</span>
+        </div>
+      </div>
+      <button class="secondary" onclick="startExam('quick')">${t("resume")}</button>
+    </div>`;
+}
+
+function moduleGrid() {
+  const soon = [
+    ["math", "\u03A3", t("modMathDesc")],
+    ["physics", "\u0192", t("modPhysicsDesc")],
+    ["stats", "\u0025", t("modStatsDesc")]
+  ];
+  return `
+    <div class="section-head">
+      <h3>${t("modules")}</h3>
+      <p class="muted-note">${t("modulesNote")}</p>
+    </div>
+    <div class="module-grid">
+      <div class="module-card is-active" onclick="startExam('quick')" role="button" tabindex="0">
+        <div class="module-icon">A</div>
+        <h4>Amirnet</h4>
+        <p>${t("modAmirnetDesc")}</p>
+      </div>
+      ${soon.map(([key, icon, desc]) => `
+        <div class="module-card is-soon" aria-disabled="true">
+          <div class="module-icon">${icon}</div>
+          <h4>${labelModule(key)} <span class="badge badge-soon">${t("comingSoon")}</span></h4>
+          <p>${desc}</p>
+        </div>`).join("")}
+    </div>`;
+}
+
+function labelModule(key) {
+  const en = { math: "Mathematics", physics: "Physics", stats: "Statistics" };
+  const he = { math: "מתמטיקה", physics: "פיזיקה", stats: "סטטיסטיקה" };
+  return (state.lang === "he" ? he : en)[key] || key;
+}
+
 function renderHome() {
   clearInterval(state.timer);
   app.innerHTML = `
-    <section class="hero">
-      <div class="card">
-        <div class="eyebrow">${t("publicBeta")}</div>
-        <h2>${t("headline")}</h2>
-        <p>${t("intro")}</p>
-        <div class="actions">
-          <button onclick="startExam('full')">${t("startFull")}</button>
-          <button class="secondary" onclick="startExam('quick')">${t("quick")}</button>
+    <section>
+      ${continueLearningCard()}
+      <div class="hero" style="margin-top: var(--sp-5)">
+        <div class="card">
+          <div class="eyebrow">${t("publicBeta")}</div>
+          <h2>${t("headline")}</h2>
+          <p>${t("intro")}</p>
+          <div class="actions">
+            <button onclick="startExam('full')">${t("startFull")}</button>
+            <button class="secondary" onclick="startExam('quick')">${t("quick")}</button>
+          </div>
+          <label class="toggle"><input id="untimed" type="checkbox" /> ${t("untimed")}</label>
         </div>
-        <label class="toggle"><input id="untimed" type="checkbox" /> ${t("untimed")}</label>
-      </div>
-      <div class="card">
-        <div class="eyebrow">${t("blueprint")}</div>
-        <div class="stats-grid mini-grid">
-          <div class="stat">${t("full")}<strong>27</strong><span>${t("questions")}</span></div>
-          <div class="stat">${t("quick")}<strong>17</strong><span>${t("questions")}</span></div>
-          <div class="stat">${t("ability")}<strong>1–5</strong><span>${t("adaptive")}</span></div>
-          <div class="stat">${t("score")}<strong>50–150</strong><span>${t("clamped")}</span></div>
+        <div class="card">
+          <div class="eyebrow">${t("blueprint")}</div>
+          <div class="stats-grid mini-grid">
+            <div class="stat">${t("full")}<strong>27</strong><span>${t("questions")}</span></div>
+            <div class="stat">${t("quick")}<strong>17</strong><span>${t("questions")}</span></div>
+            <div class="stat">${t("ability")}<strong>1–5</strong><span>${t("adaptive")}</span></div>
+            <div class="stat">${t("score")}<strong>50–150</strong><span>${t("clamped")}</span></div>
+          </div>
         </div>
       </div>
+      <div style="margin-top: var(--sp-7)">${moduleGrid()}</div>
     </section>`;
 }
 
@@ -357,7 +472,7 @@ function renderExam() {
       ${item.passage ? `<div class="passage english-content"><strong>${item.passageTitle}</strong><br><br>${item.passage}</div>` : ""}
       <div class="question-text english-content">${item.question}</div>
       <div class="options english-content">
-        ${item.options.map((opt, i) => `<button class="option ${selected === i ? "selected" : ""}" onclick="choose(${i})"><span class="option-num">${i + 1}</span><span>${opt}</span></button>`).join("")}
+        ${item.options.map((opt, i) => `<button class="option ${selected === i ? "selected" : ""}" onclick="choose(${i})"><span class="option-letter">${String.fromCharCode(65 + i)}</span><span>${opt}</span></button>`).join("")}
       </div>
       ${renderDebugPanel(item)}
       <div class="nav">
@@ -551,6 +666,41 @@ function placementBlock(result) {
     </div>`;
 }
 
+// Reads already-computed byType accuracy to surface the weakest and
+// strongest area, plus the already-computed average pace. No scoring
+// or adaptive logic here — purely a presentational summary.
+function recommendationsBlock(byType) {
+  const entries = Object.entries(byType)
+    .filter(([, d]) => d.total > 0)
+    .map(([type, d]) => ({ type, acc: percent(d.correct, d.total) }));
+  if (!entries.length) return "";
+  entries.sort((a, b) => a.acc - b.acc);
+  const weakest = entries[0];
+  const strongest = entries[entries.length - 1];
+  const t1 = computeTimeAnalysis();
+
+  const rows = [];
+  rows.push(`<div class="recommend-row"><span class="recommend-icon">↑</span><div>${t("recWeak")} <strong>${labelType(weakest.type)}</strong> (${weakest.acc}%) ${t("recWeakTail")}</div></div>`);
+  if (strongest.type !== weakest.type) {
+    rows.push(`<div class="recommend-row"><span class="recommend-icon">✓</span><div>${t("recStrong")} <strong>${labelType(strongest.type)}</strong> (${strongest.acc}%) — ${t("recStrongTail")}</div></div>`);
+  }
+  if (t1) {
+    rows.push(`<div class="recommend-row"><span class="recommend-icon">⏱</span><div>${t("recPace")} ${formatTime(Math.floor(t1.avgMs / 1000))} ${t("recPaceTail")}</div></div>`);
+  }
+  return `<h3>${t("recommendations")}</h3><div class="recommend-list">${rows.join("")}</div>`;
+}
+
+function nextStepBlock() {
+  return `
+    <div class="next-step">
+      <p class="muted-note">${t("nextStepNote")}</p>
+      <div class="actions" style="margin:0">
+        <button class="secondary" onclick="renderHome()">${t("backHome")}</button>
+        <button onclick="startExam('quick')">${t("practiceAgain")}</button>
+      </div>
+    </div>`;
+}
+
 function renderResults(result) {
   const byType = state.items.reduce((acc, item, i) => {
     acc[item.type] ||= {total:0, correct:0};
@@ -575,7 +725,9 @@ function renderResults(result) {
         <div class="stat">${t("accuracy")}<strong>${result.accuracy}%</strong></div>
         <div class="stat">${t("confidence")}<strong>${result.confidence}</strong></div>
       </div>
+
       ${placementBlock(result)}
+
       <div class="stats-grid">
         <div class="stat">${t("answered")}<strong>${result.answered}</strong></div>
         <div class="stat">${t("unanswered")}<strong>${result.unanswered}</strong></div>
@@ -584,6 +736,9 @@ function renderResults(result) {
       </div>
       <p class="muted-note">${t("estimateNote")}</p>
       <p class="muted-note">${t("scoreExplain")}</p>
+
+      ${timeAnalysisBlock()}
+
       <h3>${t("byType")}</h3>
       <div class="review metric-list">
         ${Object.entries(byType).map(([type, data]) => progressRow(labelType(type), data.correct, data.total)).join("")}
@@ -594,7 +749,9 @@ function renderResults(result) {
       </div>
       <h3>${t("abilityPath")}</h3>
       <div class="ability-path">${abilityPath()}</div>
-      ${timeAnalysisBlock()}
+
+      ${recommendationsBlock(byType)}
+
       <h3>${t("review")}</h3>
       <div class="review">
         ${state.items.map((item, i) => {
@@ -603,13 +760,14 @@ function renderResults(result) {
           return `<div class="review-item">
             <div><strong>Q${i+1} · ${labelType(item.type)} · ${t("difficulty")} ${item.difficulty}</strong> <span class="${ok ? 'good' : 'bad'}">${ok ? t("correct") : t("incorrect")}</span></div>
             <div class="english-content review-question">${item.question}</div>
-            <div style="color:var(--muted);margin-top:6px">${t("yourAnswer")}: ${user === null || user === undefined ? t("unansweredText") : item.options[user]}</div>
-            <div style="color:var(--muted)">${t("correctAnswer")}: ${item.options[item.answer]}</div>
+            <div style="color:var(--ink-soft);margin-top:6px">${t("yourAnswer")}: ${user === null || user === undefined ? t("unansweredText") : item.options[user]}</div>
+            <div style="color:var(--ink-soft)">${t("correctAnswer")}: ${item.options[item.answer]}</div>
             <div style="margin-top:6px">${item.explanation}</div>
           </div>`;
         }).join("")}
       </div>
-      <div class="actions"><button onclick="renderHome()">${t("backHome")}</button></div>
+
+      ${nextStepBlock()}
     </section>`;
 }
 
